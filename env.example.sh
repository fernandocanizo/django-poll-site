# Edit appropriately and rename this file to `.env`

export DEBUG=True
export SECRET_KEY='some secret hard to guess string'


# You must have created the virtual environment prior to activate it
. /path/to/your/virtual/environment/bin/activate

# let manage.py into PATH
export PATH="/path/to/your/project/root:${PATH}"
